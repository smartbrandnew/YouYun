说明:
   本目录中的配置文件会覆盖cli-config下对应的cli-*-define.xml配置项,请用户复制对应的配置文件,然后修改
   对应的监测项,保存即可,程序会自动加载的.
备注:
   修改保存完后的xml文件扩展名只要以.xml开头(比如.xml1),系统都能识别并加载,否则系统自动过滤掉,不会加载的.
   为了便于交流和理解,建议用户不要随意更改xml扩展名称(使用默认的.xml扩展名).